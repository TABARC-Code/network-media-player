# --- NETWORK CONFIG ---
# Your machine/server LAN IP (e.g. 192.168.1.50). Do not use localhost. APPRECIATE EASY MISTAAKE. Keep this stuff secret and do not run on a bloody open system.
HOST_IP=192.168.1.XX

# --- SPOTIFY API ---
# Get these from developer.spotify.com
SPOTIPY_CLIENT_ID=your_id_here
SPOTIPY_CLIENT_SECRET=your_secret_here
SPOTIPY_REDIRECT_URI=http://localhost:5000/callback

# --- FLASK CONFIG ---
FLASK_SECRET_KEY=super_secret_key_change_me
MEDIA_ROOT=/app/media

# --- SPOTIFY CACHE PATH (inside container) ---
SPOTIFY_CACHE_PATH=/app/.cache_volume/.spotify_cache

# --- PLAYBACK TUNING ---
# Grace period prevents Chromecast reporting IDLE right after play starts.
PLAYBACK_GRACE_SECONDS=4.0

# Monitor poll frequency. Lower means more responsive, higher means less CPU churn.
PLAYBACK_POLL_SECONDS=2.0

# Stop timeout means we do not deadlock the queue forever if a device never reports stopped.
PLAYBACK_STOP_TIMEOUT_SECONDS=8.0
